#include <iostream>
#include <algorithm>
using namespace std;
 
int main()
{
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	int n, k, l, c, d, p, nl, np;
	cin>>n>>k>>l>>c>>d>>p>>nl>>np;
	int q=(k*l)/nl;
	int w=(c*d);
	int e=p/np;
	cout<<min(q,min(w,e))/n;
	
	return 0; 
}
